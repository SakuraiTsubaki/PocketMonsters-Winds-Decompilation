# Repository Structure

Create purpose-based areas only when real verified work exists: `docs/`, `manifests/`, `src/`, `data/`, `assets/`, `tools/`, `tests/`, `patches/`. No speculative empty trees, structure-version roots, migration-history trees, or duplicate canonical homes.