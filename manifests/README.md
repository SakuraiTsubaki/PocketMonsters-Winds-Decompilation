# Manifests

Track only verified or explicitly unverified records with logical IDs, paths, source/provenance, hashes when available, generation method, verification state, and known differences. Never fabricate missing target metadata.