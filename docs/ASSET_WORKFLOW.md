# Asset Workflow

Only verified-source assets enter the canonical workflow: identify source → reproducible acquisition/extraction → provenance/hash → reviewable representation → defined conversion if needed → byte/hash dedupe → manifest → validation.